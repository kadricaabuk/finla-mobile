# Copy to .maestro/.env (gitignored) for local runs:
#   cp .maestro/.env.example .maestro/.env
#
# CI passes these via `maestro test -e TEST_PHONE=... -e TEST_PIN=...`
# Staging user must exist with tenant linked and onboarding complete.

TEST_PHONE=5551234567
TEST_PIN=123456
